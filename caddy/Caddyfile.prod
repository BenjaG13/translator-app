:80 {
    root * /var/www/public
    encode gzip

    log {
        output file /var/log/caddy/access.log
        format json
    }

    handle /api/* {
        rewrite * /index.php
        php_fastcgi translator-backend:9000
    }

    handle {
        reverse_proxy translator-frontend:80
    }
}